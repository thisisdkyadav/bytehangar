services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bytehangar
      POSTGRES_PASSWORD: bytehangar
      POSTGRES_DB: bytehangar
    ports:
      # Host 5433 to avoid clashing with other local Postgres instances.
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bytehangar"]
      interval: 5s
      timeout: 3s
      retries: 10

  # S3-compatible backend for local dev (the "s3" driver points here)
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: bytehangar
      MINIO_ROOT_PASSWORD: bytehangar-secret
    ports:
      # Host 9100/9101 to avoid clashing with other local MinIO instances.
      - "9100:9000"
      - "9101:9001"
    volumes:
      - miniodata:/data

volumes:
  pgdata:
  miniodata:
